public boolean in1020(int a, int b) {
  return ((a >= 10 && a <= 20) || (b >= 10 && b <= 20));
}

//this page's control mechanisms aren't working properly. Writing the same code logic,
//but with only slightly different indentation, or order leads to errors.